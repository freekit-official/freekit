import 'package:flutter/material.dart';

class DashboardMobileWebView extends StatelessWidget {
  const DashboardMobileWebView({super.key});

  @override
  Widget build(BuildContext context) {
    return const Text('Mobile Web');
  }
}
