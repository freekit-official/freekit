import 'package:flutter/material.dart';

class DesktopTools extends StatelessWidget {
  const DesktopTools({super.key});

  @override
  Widget build(BuildContext context) {
    return const Scaffold();
  }
}
